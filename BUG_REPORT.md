# รายงานข้อผิดพลาดโค้ด Tetris

## ข้อผิดพลาดที่พบ 🐛

### 1. **ปัญหา Ghost Piece Loop ที่ไม่สมบูรณ์** ⚠️
**ตำแหน่ง:** บรรทัด 660-661
```javascript
let maxIterations = ROWS; // ป้องกัน infinite loop
 while (maxIterations-- > 0 && canMove(ghostPiece, 0, 1)) {
```

**ปัญหา:** โค้ดไม่มีการเพิ่ม ghostPiece.y ภายใน while loop ที่ถูกต้อง
- ตัวแปร `maxIterations` สร้างแล้วแต่ไม่ได้ใช้อย่างเหมาะสม

**วิธีแก้:** ควรเป็น
```javascript
while (maxIterations-- > 0 && canMove(ghostPiece, 0, 1)) {
  ghostPiece.y++;
}
```

---

### 2. **ตรวจสอบขอบบนของกระดานไม่ถูกต้อง** ⚠️
**ตำแหน่ง:** บรรทัด 678-679
```javascript
// ตรวจสอบขอบบน (อนุญาตให้มาจากบน)
if (newY < 0) return false;
```

**ปัญหา:** บล็อคจะไม่สามารถเข้ามาจากบนของกระดาน ซึ่งเป็นพฤติกรรมปกติของ Tetris
- ในเกม Tetris ปกติ บล็อคควรสามารถอยู่นอกกระดาน (y < 0) ได้ในช่วงเริ่มต้น
- ควรลบการตรวจสอบนี้ หรือเพิ่มเงื่อนไขพิเศษ

**วิธีแก้:** ลบบรรทัดนี้ออก หรือเปลี่ยนเป็น
```javascript
// ลบการตรวจสอบ newY < 0 ออก ให้บล็อคเข้าจากบนได้
```

---

### 3. **ไม่มีการตรวจสอบ board[newY] ก่อนการเข้าถึง** ⚠️
**ตำแหน่ง:** บรรทัด 683
```javascript
if (board[newY] && board[newY][newX]) return false;
```

**ปัญหา:** เมื่อ `newY < 0` ไม่จะมีปัญหา แต่การตรวจสอบนี้ไม่ได้ครอบ edge case ทั้งหมด

**วิธีแก้:** ควรมีการตรวจสอบที่สมบูรณ์
```javascript
if (newY >= 0 && newY < ROWS && board[newY] && board[newY][newX]) return false;
```

---

## สรุป
- **ข้อผิดพลาดจำนวน:** 3 ข้อ
- **ระดับความสำคัญ:** ปานกลาง
- **ผลกระทบ:** 
  - บล็อกอาจไม่แสดง ghost piece ได้ถูกต้อง
  - ระบบการชนกันอาจมีข้อผิดพลาด
